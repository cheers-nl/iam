{
  "name": "infra",
  "version": "0.1.0",
  "scripts": {
    "clean:compiled": "rm -rf dist bin/*.js bin/*.d.ts lib/*.js lib/*.d.ts test/*.js test/*.d.ts",
    "build": "tsc",
    "pretest": "npm run clean:compiled",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/jest": "^30",
    "@types/node": "^24.10.1",
    "aws-cdk": "2.1121.0",
    "esbuild": "^0.28.0",
    "jest": "^30",
    "ts-jest": "^29",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.252.0",
    "constructs": "^10.5.0"
  }
}
